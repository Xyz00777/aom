#!/usr/bin/env bash
# pre-commit hook: delegates to pre-commit framework
# See .pre-commit-config.yaml for configured hooks
set -e

echo "[pre-commit] running pre-commit checks..."
uv run pre-commit run "$@"