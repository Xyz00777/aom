#!/usr/bin/env bash
# pre-push hook: delegates to pre-commit framework (pre-push stage)
# See .pre-commit-config.yaml for configured hooks (pytest at pre-push stage)
set -e

echo "[pre-push] running pre-commit pre-push checks..."
uv run pre-commit run --hook-stage push "$@"