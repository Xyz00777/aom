"""Non-interactive renderers: format adapters for CI/scripting use.

Each module here implements the :class:`Renderer` Protocol but emits
a structured artifact instead of streaming a TUI. See
``ARCHITECTURE.md §3``.
"""
