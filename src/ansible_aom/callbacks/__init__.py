"""AOM callback plugins for ansible-playbook integration.

This package contains callback plugins that run inside the
ansible-playbook subprocess to emit structured events for AOM's
connection tracking and other observability features.
"""
