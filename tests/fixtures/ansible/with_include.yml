---
- name: Playbook with include_tasks
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Direct task
      debug:
        msg: "Direct"
    - name: Include other tasks
      include_tasks: included_tasks.yml
